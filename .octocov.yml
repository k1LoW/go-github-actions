codeToTestRatio:
  code:
    - '**/*.go'
    - '!**/*_test.go'
  test:
    - '**/*_test.go'
comment:
  if: is_pull_request
report:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
diff:
  if: is_pull_request
  datastores:
    - artifact://${GITHUB_REPOSITORY}
